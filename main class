package core;

import adapters.DataRetriever;
import adapters.YahooFinanceDataRetriever;
import ui.ChartDisplay;
import ui.UserInputHandler;

/**
 * Entry point of the application.
 */
public class Main {
    public static void main(String[] args) {
        // Initialize components
        DataRetriever dataRetriever = new YahooFinanceDataRetriever();
        UserInputHandler userInputHandler = new UserInputHandler();
        ChartDisplay chartDisplay = new ChartDisplay();

        // Application logic
    }
}
